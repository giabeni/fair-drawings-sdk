import { SecurityService } from '../security/security.service';
import * as crypto from 'crypto';

describe('Draw Service Tests', () => {
  // Shared scope variables

  describe('Communicator tests', () => {
    test('Set invalid communicator and initiate service connection', () => {
      /** @TODO */
      expect(false).toBe(true);
    });

    test('Set valid communicator and initiate service connection', () => {
      /** @TODO */
      expect(false).toBe(true);
    });
  });

  describe('Draw Listing tests', () => {
    test('Get empty list of draws', () => {
      /** @TODO */
      expect(false).toBe(true);
    });

    test('Create some draws', () => {
      /** @TODO */
      expect(false).toBe(true);
    });

    test('Get filled list of draws', () => {
      /** @TODO */
      expect(false).toBe(true);
    });
  });

  describe('Draw Events tests', () => {
    test('Try to watch invalid draw', () => {
      /** @TODO */
      expect(false).toBe(true);
    });

    test('Watch valid draw', () => {
      /** @TODO */
      expect(false).toBe(true);
    });

    test('Get filled list of draws', () => {
      /** @TODO */
      expect(false).toBe(true);
    });
  });
});
